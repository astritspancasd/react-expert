import React from 'react';
import { RegisterForm, PageContainer } from '~/components';

export const Register = () => {
  return (
    <PageContainer>
      <RegisterForm />
    </PageContainer>
  );
};
